#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/

pre-commit:
  parallel: true
  jobs:
    - name: prettier
      run:
        sh -c 'pnpx prettier --ignore-unknown --check "{staged_files}"; code=$?; pnpx prettier
        --ignore-unknown --write "{staged_files}"; exit $code'
    - name: lint
      run: pnpx oxlint --fix "{staged_files}"
    - name: "turbo: build + test + type-check"
      run: pnpx turbo run --no-daemon build types:check test
      glob: "{*.ts,*.tsx,tsconfig.json}"
